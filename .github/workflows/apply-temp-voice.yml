name: Obsolete temporary voice workflow

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Nothing to patch
        run: echo "Temporary voice rooms are already integrated into bot.py."
